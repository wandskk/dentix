export type HeroStat = {
  icon: string;
  text: string;
};

export type SocialProofUser = {
  id: number;
  name: string;
  role: string;
};

export type FloatingElement = {
  text: string;
  subtitle: string;
};
